function MotoTrak_Set_Custom_Variants(handles

if isfield(handles,'variant')                                               %If any custom variant is enabled...
    switch lower(handles.variant)                                           %Switch between the various recognized variants.
    end
end